{% extends 'base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
<div class="container my-4">
    <h1>Comment</h1>
    <a href="{{ path('app_back_comment_index') }}"class="btn btn-outline-primary float-end">back to list</a>

    <table class="table table-hover mt-4">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ comment.content }}</td>
            </tr>
        </tbody>
    </table>


    <a href="{{ path('app_back_comment_edit', {'id': comment.id}) }}"class="btn btn-warning">edit</a>

    {{ include('back/comment/_delete_form.html.twig') }}
</div>
{% endblock %}
